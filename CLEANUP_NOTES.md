# הערות ניקוי הפרויקט

## שינויים שבוצעו לאחרונה

### מעבר מלא ללוחות שפיצר (במאגר הנתונים)

הפרויקט עודכן לעבוד אך ורק עם לוחות שפיצר מהמסד נתונים MongoDB, ללא תלות בקבצי CSV:

1. **הוסרו קבצים/מודולים**:
   - `server/models/MortgageData.js` - מודל נתוני המשכנתאות הישן
   - `server/routes/mortgageData.js` - נתיבי API לקבצי CSV
   - `copy-mortgage-data.js` - סקריפט העתקת קבצי CSV
   - `client/src/utils/mortgageTable.js` - מודול חישובים מבוסס CSV

2. **עודכנו קבצים**:
   - `client/src/utils/calculations.js` - עודכן לשימוש ב-mortgageCalculations.js במקום mortgageTable.js
   - `server/server.js` - הוסרו התייחסויות לנתיבים ושירותים של קבצי CSV
   - `server/controllers/calculationController.js` - הוסרו פונקציות יבוא ועבודה עם קבצי CSV

## קבצים ותיקיות להסרה

הקבצים ותיקיות הבאים הם כפילויות או שאינם בשימוש יותר, וניתן להסירם כאשר אף אחד אינו משתמש בהם:

1. **תיקיית `public/`** - תיקייה זו כבר לא נדרשת כי אנחנו משתמשים בלוחות שפיצר מהמסד במקום קבצי CSV.
   - מומלץ לנסות להסיר את התיקייה עם `Remove-Item -Recurse -Force public` כאשר השרת אינו פועל.

2. **תיקיית `server/data/`** - תיקיית זו הכילה קבצי CSV של נתוני משכנתאות שאינם נדרשים עוד.
   - ניתן להסיר את התיקייה עם `Remove-Item -Recurse -Force server/data` כאשר השרת אינו פועל.

3. **קובץ `fix-src.js`** - קובץ שעזר לארגן את הפרויקט ואינו נדרש עוד.

4. **תיקיית `backup/`** - תיקייה זו הוספה כגיבוי של תיקיית `src/` הישנה וניתן להסירה בבטחה לאחר שווידאת שכל הקבצים הדרושים זמינים בתיקיית `client/src/`.

## עדכון קבצי הגדרות

1. **עדכון `webpack.config.js`** - ודא שכל ההפניות הן לתיקיית `client/` ולא לתיקיות ישנות.

2. **עדכון `package.json`** - בדוק אם נדרש לעדכן סקריפטים:
   - הסר סקריפט `copy-data` אם הוא עדיין קיים מכיוון שהוא אינו נדרש עוד.
   - ודא שהסקריפטים האחרים מפנים לתיקיות הנכונות.

## שינויים שהושלמו

1. **הסרת תיקיית `src/`** - בוצע
2. **הסרת תיקיית `apartment-investment-calculator/`** הפנימית - בוצע
3. **עדכון `PROJECT_ARCHITECTURE_GUIDE.md`** - בוצע
4. **עדכון `README.md`** - בוצע
5. **מעבר מלא ללוחות שפיצר** - בוצע
6. **הסרת המודלים והקבצים הישנים המבוססים על CSV** - בוצע

## מחשבות לעתיד

1. **שינוי המבנה ל-single package** - שקול לאחד את קבצי ה-package.json של הלקוח והשרת ליצירת מבנה פרויקט פשוט יותר.

2. **שדרוג ל-TypeScript** - שקול להסב את הפרויקט ל-TypeScript לקבלת יתרונות של בדיקות סוג סטטיות והשלמה אוטומטית טובה יותר. 