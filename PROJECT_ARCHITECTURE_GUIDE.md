# מדריך ארכיטקטורת הפרויקט - מחשבון השקעות נדל"ן

## מבנה כללי

הפרויקט מחולק לשני חלקים עיקריים:
- **צד לקוח (client)**: אפליקציית React שרצה בדפדפן
- **צד שרת (server)**: שרת Node.js עם Express ו-MongoDB

מבנה הפרויקט המעודכן:
```
apartment-investment-calculator/
├── client/                  # אפליקציית צד לקוח (React)
│   ├── src/                 # קוד המקור של צד הלקוח
│   │   ├── components/      # רכיבי React 
│   │   ├── services/        # שירותים לתקשורת עם ה-API
│   │   └── utils/           # פונקציות עזר וחישובים
│   ├── public/              # קבצים סטטיים
│   ├── package.json         # הגדרות והתלויות של צד הלקוח
│   └── webpack.config.js    # הגדרות בנייה של צד הלקוח
│
├── server/                  # שרת (Node.js, Express, MongoDB)
│   ├── config/              # הגדרות תצורה
│   ├── controllers/         # בקרים להתנהגות לוגית
│   ├── data/                # קבצי נתונים 
│   ├── models/              # מודלים של MongoDB
│   ├── routes/              # הגדרות נתיבים (routes)
│   ├── middlewares/         # middleware שונים
│   ├── server.js            # קובץ כניסה ראשי של השרת
│   ├── import-schedules.js  # סקריפט ליבוא לוחות שפיצר
│   └── package.json         # הגדרות והתלויות של השרת
│
├── schedules_diyur_all.json # קובץ נתוני לוחות שפיצר
├── .gitignore               # קבצים להתעלמות ב-Git
├── package.json             # הגדרות פרויקט ראשי
└── README.md                # תיעוד ראשי של הפרויקט
```

## שינויים עיקריים במערכת

### 1. שינוי שם קולקציית MongoDB
- המערכת עברה משימוש בקולקציית `investments` לקולקציית `deals`
- כל הקוד עודכן להשתמש בשם החדש
- נוסף סקריפט `fix-collections.js` להעברת נתונים מהקולקציה הישנה לחדשה

### 2. מערכת לוחות שפיצר
- נוספה מערכת חישוב משכנתאות חדשה המבוססת על לוחות שפיצר מהמסד
- לוחות שפיצר מאפשרים חישוב מדויק יותר של החזרי משכנתא והריבית

## פירוט רכיבי המערכת

### 1. מודלים במסד הנתונים

#### 1.1 Deal.js (לשעבר Investment.js)
- מודל לשמירת עסקאות נדל"ן והחישובים שלהם
- כולל נתוני קלט, תוצאות חישוב ותחזית

#### 1.2 Schedule.js (חדש)
- מודל לשמירת לוחות שפיצר למשכנתאות
- כולל:
  - `purpose`: מטרת ההלוואה (דיור, מסחרי, וכו')
  - `years`: תקופת ההלוואה בשנים
  - `interest`: שיעור הריבית השנתית
  - `loanAmount`: סכום ההלוואה הבסיסי
  - `monthlyPayments`: מערך של תשלומים חודשיים
- מכיל מתודות לחיפוש, התאמה וחישוב ממוצעים

### 2. ממשקי API חדשים

#### 2.1 /api/schedules
- `/check`: בדיקת זמינות לוחות שפיצר במערכת
- `/adjustedSchedule`: קבלת לוח שפיצר מותאם לפרמטרים ספציפיים
- `/averages`: קבלת ממוצעי תשלומים בלבד

#### 2.2 /api/deals (לשעבר /api/investments)
- עודכן להשתמש בקולקציית `deals` במקום `investments`
- ממשק זהה: 
  - `GET /api/deals`: מקבל את כל העסקאות
  - `GET /api/deals/:id`: מקבל עסקה לפי מזהה
  - `POST /api/deals`: יוצר עסקה חדשה
  - `PUT /api/deals/:id`: מעדכן עסקה קיימת
  - `DELETE /api/deals/:id`: מוחק עסקה

### 3. שירותי צד לקוח

#### 3.1 scheduleService.js (חדש)
- שירות לתקשורת עם API של לוחות שפיצר
- כולל פונקציות:
  - `checkSchedulesAvailability`: בדיקת זמינות לוחות
  - `getAdjustedSchedule`: קבלת לוח מותאם
  - `getScheduleAverages`: קבלת ממוצעי תשלומים

#### 3.2 mortgageCalculations.js (חדש)
- מערכת חישוב חדשה המבוססת על לוחות שפיצר
- כולל פונקציות:
  - `calculateMonthlyPaymentFromSchedule`: חישוב תשלום חודשי לפי לוח שפיצר
  - `getFullPaymentSchedule`: קבלת לוח תשלומים מלא
  - `fallbackPaymentCalculation`: חישוב גיבוי במקרה של שגיאה

### 4. סקריפטים מיוחדים

#### 4.1 import-schedules.js
- סקריפט ליבוא לוחות שפיצר מקובץ JSON למסד הנתונים
- תומך בדגל `--force` למחיקת נתונים קיימים ויבוא מחדש

#### 4.2 fix-collections.js
- סקריפט להעברת נתונים מקולקציית `investments` ל-`deals`
- בודק קיום הקולקציות, מעתיק נתונים ומדווח על התוצאות

## יתרונות המערכת החדשה

### 1. מערכת לוחות שפיצר
- **דיוק**: חישובים מדויקים יותר של החזרי משכנתא וריבית
- **גמישות**: אפשרות להשתמש בנתונים מעודכנים ללא שינוי קוד
- **מהימנות**: חישובים מבוססים על לוחות שפיצר אמיתיים במקום חישובים מתמטיים
- **תחזיות ארוכות טווח**: אפשרות לחשב תחזיות ארוכות טווח עם דיוק גבוה

### 2. ארכיטקטורת מיקרו-שירותים
- הפרדה בין שירותי עסקאות נדל"ן לשירותי חישובי משכנתא
- תחזוקה פשוטה יותר ואפשרויות הרחבה בעתיד

## מדריך להפעלת המערכת

### 1. התקנה ראשונית
1. התקן את כל התלויות: `npm run install:all`
2. צור קובץ `.env` בתיקיית `server` עם משתני הסביבה הדרושים
3. הכן את מסד הנתונים MongoDB

### 2. יבוא נתוני לוחות שפיצר
1. וודא שקובץ `schedules_diyur_all.json` נמצא בתיקיית השורש
2. הרץ את הסקריפט: `node server/import-schedules.js`
3. לאחר היבוא, בדוק את הנתונים באמצעות API: `/api/schedules/check`

### 3. הפעלת המערכת
- פיתוח (צד לקוח ושרת יחד): `npm run dev`
- שרת בלבד: `npm run server`
- לקוח בלבד: `npm run client`

## התפתחויות עתידיות

1. **מערכת משתמשים**: הוספת אימות והרשאות
2. **ניהול פורטפוליו**: אפשרות לנהל מספר עסקאות במקביל
3. **תצוגות מורחבות**: דוחות מורחבים וניתוח מעמיק
4. **API להשוואת נכסים**: השוואה בין מספר אפשרויות השקעה
5. **אפליקציית מובייל**: גישה למערכת גם דרך מכשירים ניידים 