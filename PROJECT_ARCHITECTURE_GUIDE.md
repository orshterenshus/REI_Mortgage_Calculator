# מדריך ארכיטקטורת הפרויקט
# מחשבון השקעות נדל"ן - ארכיטקטורת פרויקט

## מבנה כללי

הפרויקט מחולק לשני חלקים עיקריים:
- **צד לקוח (client)**: אפליקציית React שרצה בדפדפן
- **צד שרת (server)**: שרת Node.js עם Express ו-MongoDB

מבנה הפרויקט:
```
apartment-investment-calculator/
├── client/                  # אפליקציית צד לקוח (React)
│   ├── src/                 # קוד המקור של צד הלקוח
│   ├── public/              # קבצים סטטיים
│   ├── package.json         # הגדרות והתלויות של צד הלקוח
│   └── webpack.config.js    # הגדרות בנייה של צד הלקוח
│
├── server/                  # שרת (Node.js, Express, MongoDB)
│   ├── config/              # הגדרות תצורה
│   ├── controllers/         # בקרים להתנהגות לוגית
│   ├── data/                # קבצי נתונים (CSV)
│   ├── models/              # מודלים של MongoDB
│   ├── routes/              # הגדרות נתיבים (routes)
│   ├── middlewares/         # middleware שונים
│   ├── server.js            # קובץ כניסה ראשי של השרת
│   └── package.json         # הגדרות והתלויות של השרת
│
├── .gitignore               # קבצים להתעלמות ב-Git
├── package.json             # הגדרות פרויקט ראשי
├── README.md                # תיעוד ראשי של הפרויקט
└── copy-mortgage-data.js    # סקריפט העתקת קבצי CSV
```

## פירוט קבצים ותיקיות

### 1. קבצים בתיקיית השורש

#### 1.1 package.json

**תפקיד**: קובץ תצורה ראשי של npm המגדיר את הפרויקט, התלויות והסקריפטים להרצה.

**פירוט הגדרות**:
- `name`, `version`, `description`: מידע בסיסי
- `scripts`: פקודות שניתן להריץ:
  - `start`: מריץ את השרת בלבד
  - `client`: מריץ את צד הלקוח בלבד
  - `server`: מריץ את השרת במצב פיתוח עם nodemon (הפעלה מחדש אוטומטית)
  - `dev`: מריץ את השרת והלקוח במקביל
  - `install:all`: מתקין את כל התלויות בפרויקט
  - `copy-data`: מעתיק את קבצי ה-CSV למיקום הנכון
  - `build`: בונה את צד הלקוח לייצור
- `dependencies`: חבילת `concurrently` שמאפשרת להריץ מספר פקודות במקביל 

**למה ככה?** 
דרוש קובץ package.json ראשי כדי לנהל את הפרויקט כולו ולאפשר הרצה נוחה של הלקוח והשרת במקביל.

#### 1.2 README.md

**תפקיד**: תיעוד ראשי של הפרויקט שמספק מידע כללי, הוראות התקנה והפעלה.

**פירוט התוכן**:
- מידע כללי על המיזם
- מבנה הפרויקט 
- פירוט תכונות המערכת
- הוראות התקנה ושימוש
- תיעוד ה-API
- תיאור טכנולוגיות

**למה ככה?**
README הוא הדבר הראשון שמשתמשים רואים בפרויקט. חשוב שיהיה תיעוד מסודר המסביר איך להתקין ולהפעיל את המערכת.

#### 1.3 .gitignore

**תפקיד**: להגדיר אילו קבצים Git צריך להתעלם מהם ולא לעקוב אחריהם.

**פירוט תוכן**:
- `node_modules`: תיקיות מודולים של npm
- קבצי `.env`: קבצי סביבה המכילים מידע רגיש
- קבצי בנייה וגרסאות: `dist`, `build`
- קבצי לוג
- קבצי עורכי קוד
- קבצי מערכת הפעלה

**למה ככה?**
נדרש להתעלם מקבצים מסוימים כמו node_modules (גדולים מאוד) וקבצי .env (מכילים סיסמאות ומידע רגיש).

#### 1.4 copy-mortgage-data.js

**תפקיד**: סקריפט המעתיק את קבצי נתוני המשכנתאות (CSV) מתיקיית public לתיקיית server/data.

**איך זה עובד**:
1. בודק אם תיקיית server/data קיימת, אם לא - יוצר אותה
2. מגדיר רשימה של קבצי CSV להעתקה
3. מעתיק את הקבצים אחד-אחד ומדווח על התהליך

**למה ככה?**
הפרדת קוד הלקוח והשרת דורשת העתקת קבצי הנתונים כדי שהשרת יוכל לגשת אליהם. עדיף לאוטומטיזציה של תהליך זה.

### 2. תיקיית השרת (server)

#### 2.1 server/package.json

**תפקיד**: מגדיר את התלויות והסקריפטים הספציפיים לצד השרת.

**פירוט הגדרות**:
- `dependencies`: 
  - `express`: שרת HTTP
  - `mongoose`: חיבור ל-MongoDB
  - `cors`: מאפשר בקשות מדומיינים שונים
  - `dotenv`: טעינת משתני סביבה מקובץ
  - `morgan`: לוגים של בקשות HTTP
  - `csv-parser`: קריאת קבצי CSV

**למה ככה?**
חלק ממבנה פרויקט מודולרי. כל חלק (לקוח/שרת) מנהל תלויות משלו.

#### 2.2 server/server.js

**תפקיד**: קובץ הכניסה הראשי של השרת. מגדיר את שרת ה-Express, מחבר למסד הנתונים, ומגדיר middleware ו-routes.

**איך זה עובד**:
1. טוען את משתני הסביבה מ-.env
2. יוצר מופע של Express
3. מחבר ל-MongoDB
4. מגדיר middleware:
   - `express.json()`: מפענח גוף בקשה בפורמט JSON
   - `cors()`: מטפל בשיתוף משאבים בין מקורות שונים
   - `morgan()`: רישום לוג של בקשות
5. מגדיר נתיבים (routes)
6. מגדיר middleware לטיפול בשגיאות
7. מפעיל את השרת

**למה ככה?**
מבנה סטנדרטי לאפליקציית Express. מאורגן בצורה הגיונית מההתחלה ועד סוף הטיפול בבקשה.

#### 2.3 server/config/db.js

**תפקיד**: אחראי על התחברות ל-MongoDB.

**איך זה עובד**:
1. מייבא את Mongoose
2. מגדיר פונקציית חיבור אסינכרונית 
3. מנסה להתחבר ל-MongoDB עם פרמטרים מתאימים
4. מטפל בשגיאות במקרה של כישלון בחיבור

**למה ככה?**
מפריד את קוד החיבור למסד הנתונים לקובץ נפרד לפי עקרון ההפרדה (Separation of Concerns).

#### 2.4 server/models/

**תפקיד**: מגדיר את סכמות המודלים של MongoDB.

##### 2.4.1 User.js

**תפקיד**: מגדיר את מודל המשתמש.

**מבנה**:
- `name`: שם המשתמש 
- `email`: כתובת דוא"ל ייחודית
- `createdAt`: תאריך יצירה

**למה ככה?**
מודל בסיסי שיאפשר בעתיד ניהול משתמשים, כולל הרשמה והתחברות.

##### 2.4.2 Investment.js

**תפקיד**: מגדיר את מודל חישובי ההשקעה.

**מבנה**:
- קשר למשתמש (`user`)
- נתוני קלט: `propertyValue`, `purchaseTaxRate`, וכו'
- תוצאות חישוב: `results`
- תחזית לאורך זמן: `forecast`

**למה ככה?**
המודל מייצג במדויק את הנתונים הדרושים לחישובי ההשקעה, כולל נתוני קלט, תוצאות ותחזית עתידית.

#### 2.5 server/routes/calculations.js

**תפקיד**: מגדיר את נתיבי ה-API לחישובי השקעה.

**נתיבים מוגדרים**:
- `GET /api/calculations`: מקבל את כל ההשקעות
- `GET /api/calculations/:id`: מקבל השקעה לפי מזהה
- `POST /api/calculations`: יוצר השקעה חדשה
- `PUT /api/calculations/:id`: מעדכן השקעה קיימת
- `DELETE /api/calculations/:id`: מוחק השקעה
- `POST /api/calculations/calculate`: מבצע חישוב שרת

**למה ככה?**
עוקב אחר עקרונות REST ומחלק את האחריות - ה-routes רק מגדירים את הנתיבים והפונקציות מהבקר מטפלות בלוגיקה.

#### 2.6 server/controllers/calculationController.js

**תפקיד**: מכיל את הלוגיקה העסקית לטיפול בבקשות HTTP.

**פונקציות עיקריות**:
- פונקציות CRUD להשקעות:
  - `getInvestments`: מחזיר רשימת השקעות
  - `getInvestmentById`: מחזיר השקעה לפי מזהה
  - `createInvestment`: יוצר השקעה חדשה 
  - `updateInvestment`: מעדכן השקעה קיימת
  - `deleteInvestment`: מוחק השקעה
- פונקציות חישוב:
  - `loadMortgageDataFromCSV`: טוען נתוני משכנתא מקובץ
  - `calculateInvestment`: מבצע חישובים (לעתיד)

**למה ככה?**
הפרדת החלק שמטפל בבקשות מהחלק שמגדיר את הנתיבים. עיקרון ה-MVC (Model-View-Controller).

#### 2.7 server/data/

**תפקיד**: מכיל את קבצי ה-CSV של נתוני המשכנתאות.

**למה ככה?**
השרת צריך גישה לקבצי הנתונים כדי לבצע חישובים מדויקים של החזרי משכנתא.

### 3. תיקיית הלקוח (client)

#### 3.1 client/package.json

**תפקיד**: מגדיר את התלויות והסקריפטים הספציפיים לצד הלקוח.

**פירוט הגדרות**:
- `dependencies`: 
  - `react`, `react-dom`: ספריית React
  - `@emotion/styled`: סגנון CSS ב-JavaScript
  - `chart.js`: ליצירת גרפים ויזואליים
  - `axios`: לביצוע בקשות HTTP ל-API
  - וחבילות נוספות

**למה ככה?**
מגדיר את הספריות הספציפיות הדרושות לצד הלקוח.

#### 3.2 client/webpack.config.js

**תפקיד**: מגדיר איך לבנות את אפליקציית הלקוח.

**הגדרות מרכזיות**:
- `entry`: נקודת הכניסה לאפליקציה
- `output`: איפה לשמור את הקבצים המבונים
- `module.rules`: איך לטפל בסוגי קבצים שונים
- `resolve`: הגדרות פתרון יבוא מודולים וחלופות
- `plugins`: תוספים כמו HtmlWebpackPlugin
- `devServer`: הגדרות שרת הפיתוח כולל proxy ל-API

**למה ככה?**
Webpack הוא כלי חשוב לבניית אפליקציות React. ההגדרות מאפשרות:
1. המרת קוד JavaScript מודרני לקוד תואם דפדפנים
2. טיפול בקבצי CSS ונכסים אחרים
3. הגדרת proxy להעברת בקשות API מהלקוח לשרת בזמן פיתוח

#### 3.3 client/src/utils/api.js

**תפקיד**: מגדיר תצורת Axios לביצוע בקשות HTTP.

**פירוט תכונות**:
- יוצר מופע Axios עם הגדרה בסיסית
- מגדיר interceptors לטיפול בבקשות ותגובות
- הגדרת טיפול בשגיאות

**למה ככה?**
מרכז את כל הגדרות ה-API במקום אחד. מאפשר תוספת של לוגיקה כמו אימות (אוטנטיקציה) בעתיד.

#### 3.4 client/src/services/investmentService.js

**תפקיד**: מספק פונקציות שירות לביצוע פעולות מול API ההשקעות.

**פונקציות עיקריות**:
- `getInvestments`: מביא את כל ההשקעות
- `getInvestmentById`: מביא השקעה לפי מזהה
- `createInvestment`: יוצר השקעה חדשה
- `updateInvestment`: מעדכן השקעה קיימת
- `deleteInvestment`: מוחק השקעה
- `calculateInvestmentOnServer`: לחישוב עתידי בשרת

**למה ככה?**
הפרדת קוד הגישה ל-API מקוד ה-UI, מה שמאפשר תחזוקה פשוטה יותר ושימוש מחדש בלוגיקה.

### 4. ההגיון מאחורי חלוקת הפרויקט

#### 4.1 למה להפריד את הלקוח והשרת?

1. **הפרדת אחריות**:
   - צד לקוח: אחראי רק על ממשק המשתמש והצגת נתונים
   - צד שרת: אחראי על הלוגיקה, חישובים וגישה למסד נתונים

2. **יתרונות טכניים**:
   - סקילביליות: ניתן לשנות גודל כל חלק בנפרד
   - בטיחות: מידע רגיש נשמר בשרת בלבד
   - ביצועים: ניתן לבצע חישובים מורכבים בשרת במקום בדפדפן
   - גמישות: ניתן להחליף צד אחד בלי לשנות את השני

3. **התפתחות עתידית**:
   - אפשרות ליצור אפליקציות נוספות (אפליקציית מובייל, למשל) שמשתמשות באותו שרת
   - שילוב שירותי צד שלישי ואינטגרציות שונות

#### 4.2 למה MongoDB?

1. **מבנה נתונים גמיש**:
   - סכמה גמישה המתאימה לשמירת נתוני השקעה שיכולים להשתנות
   - שמירת מסמכים מורכבים (כמו תחזיות לאורך זמן) כמסמך אחד

2. **ביצועים**:
   - מהירות גבוהה בפעולות קריאה/כתיבה
   - יכולת לשאילתות מורכבות

3. **סקילביליות**:
   - קל להרחיב מסד נתונים MongoDB עם גידול בנפח הנתונים

#### 4.3 מבנה התיקיות בשרת

מבנה התיקיות בשרת מבוסס על עקרון ה-MVC (Model-View-Controller) המותאם לפיתוח Node.js:

- `models/`: מייצג את שכבת הנתונים (MongoDB)
- `controllers/`: מכיל את הלוגיקה העסקית
- `routes/`: מגדיר את ה-API endpoints
- `config/`: מכיל הגדרות תצורה (כמו חיבור למסד נתונים)
- `middlewares/`: כולל middleware לשימוש משותף

**למה ככה?**
ארגון זה מאפשר:
1. הפרדת אחריות ברורה
2. תחזוקה נוחה יותר
3. קוד מודולרי ושימוש חוזר
4. בדיקות קלות יותר

## שלבים הבאים

### 1. השלמת הגדרת MongoDB
- יצירת מסד נתונים MongoDB
- הגדרת קובץ .env עם פרטי חיבור

### 2. התאמת החישובים לעבודה עם השרת
- העברת לוגיקת החישוב מהלקוח לשרת
- עדכון הלקוח לביצוע בקשות לשרת במקום חישוב מקומי

### 3. הוספת אימות (בעתיד)
- מערכת הרשמה/התחברות
- הגבלת גישה להשקעות ספציפיות למשתמשים ספציפיים 